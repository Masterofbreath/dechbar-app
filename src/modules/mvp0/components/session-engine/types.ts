/**
 * Session Engine Types
 * 
 * Local types for session engine component
 * 
 * @package DechBar_App
 * @subpackage MVP0/Components/SessionEngine
 */

export type SessionState = 'idle' | 'mood-before' | 'countdown' | 'active' | 'completed' | 'abandoned';
